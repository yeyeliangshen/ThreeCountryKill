# 给编码 Agent 的须知（本仓库的固定约定）

## 一、先读工作区 skill（`.zcode/skills/`）

本仓库自带一批 skill，**按任务类型先读再动手**（用 Skill 工具加载；不要凭印象跳过）：

| 任务 | 先读 |
|---|---|
| 牌堆 / 牌区 / 摸牌重洗 / 回合结构 / 费用 / 效果结算 / 手牌上限 | `card-game` |
| HUD / 菜单 / 浮层 / 布局与缩放 / 安全区 / 键盘·手柄焦点 / 屏幕流转 | `game-ui-ux` |
| 打击感（震动、顿帧、缓动、音画反馈） | `game-feel` |
| 从零做原型、时间盒验证玩法 | `prototype-fast` |
| Phaser / PixiJS / three.js 相关 | `phaser-*` / `pixijs-rendering` / `threejs-*` |

⚠️ 血泪教训：曾经整轮会话都没读 `card-game`，结果是在**它的两条头号 pitfall**（「一张牌同时
存在于两个区域」「未播种的洗牌」）上反复踩坑——这两条本项目后来是靠模糊测试网兜住的，但如果
一开始读了 skill，会省很多事。

## 二、规则文本：**不猜**

- 技能/卡牌文本必须来自**官方或用户核对后提供的口径**。核不到、或来源互相冲突时：**标 `待核对`，
  不实现**，并写进 `docs/guozhan-roster.md`。
- 用户给的精确转述**就是权威来源**（他们常常拿不到逐字原文，只能给转述）。
- 版本差异（2019 典藏版 / 君临天下·权 / 2023 改版 / 2026 新版…）必须如实记下来，不要混用。

## 三、每次改动都要

1. `pnpm test` + `pnpm typecheck` + `pnpm build` **全绿** 再提交；
2. 在 `docs/guozhan-roster.md` 里**新增一节**记录：改了什么、口径依据、简化与待核对项
   （这份文档是本仓库唯一的「口径 + 欠账」权威清单）；
3. 提交信息**用文件或 heredoc 写**（`git commit -F-`）——**不要在命令行里带反引号**，
   bash 会把它们当命令替换执行、把消息吃掉（踩过一次）；
4. 如实报告**没验证到的部分**（例如界面改动只过了 typecheck/build、没真开浏览器）。

## 四、分支

只推 `feature/phase3`。`main` 由用户自己开 PR 合并，**不要直推 main**。

## 五、测试纪律

- 引擎的**模糊测试网**（`packages/engine/tests/fuzz.test.ts`）是本项目最重要的安全网：
  它抓过 5 类真 bug（续接队列排序、丢牌、随机源泄漏、重复弃置、判定在飞）。
  **改了引擎核心流程或新增技能后，务必跑它**，并且不要为了「让它通过」而放宽断言。
- 新增技能/卡牌：先问自己「哪一种输入会让它出错」，再写用例；能写成结构不变式（不变量）的
  就写进 fuzz。
